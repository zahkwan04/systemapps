<Window x:Class="systemapps.datatemplatetest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:systemapps"
        mc:Ignorable="d"
        Title="datatemplatetest" Height="450" Width="800">
    <Grid>

        <ContentControl Content="{Binding PersonTest}" >
            <ContentControl.ContentTemplate>
                <DataTemplate>
                    <Grid>
                    <TextBlock Text="{Binding Firstname}" FontSize="15"/>
                    </Grid>
                </DataTemplate>
            </ContentControl.ContentTemplate>
        </ContentControl>

    </Grid>
</Window>
